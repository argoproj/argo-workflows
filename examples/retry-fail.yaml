apiVersion: argoproj.io/v1alpha1
kind: Workflow
metadata:
  generateName: retry-fail-
spec:
  entrypoint: retry-fail
  templates:
    - name: retry-fail
      container:
        image: argoproj/argosay:v2
        command: [sh, -c]
        args: ["echo intentional failure; exit 1"]