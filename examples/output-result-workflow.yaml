kind: Workflow
apiVersion: argoproj.io/v1alpha1
metadata:
  generateName: output-result-
  labels:
    workflows.argoproj.io/test: "true"
  annotations:
    workflows.argoproj.io/verify: |
      assert(nodes["a"].outputs.exitCode == 0)
spec:
  entrypoint: main
  templates:
    - name: main
      dag:
        tasks:
          - name: a
            arguments:
              parameters:
                - name: p-0
                  value: "foo"
            template: pod-0
          - name: b
            template: pod-0
            dependencies:
              - a
            arguments:
              parameters:
                - name: p-0
                  value: "{{tasks.a.outputs.result}}"
    - name: pod-0
      inputs:
        parameters:
          - name: p-0
      container:
        image: argoproj/argosay:v2
        args:
          - echo
          - "{{inputs.parameters.p-0}}"
          - /dev/stdout
