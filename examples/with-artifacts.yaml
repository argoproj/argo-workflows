# This example demonstrates the ability to pass artifacts
# from one step to the next.
apiVersion: argoproj.io/v1alpha1
kind: Workflow
metadata:
  generateName: artifact-passing-
spec:
  entrypoint: artifact-example
  templates:
  - name: artifact-example
    steps:
    - - name: generate-artifact
        template: generate-artifacts
    - - name: process-artifact
        template: process-message
        withParam: "{{steps.generate-artifact.outputs.result}}"
        arguments:
          parameters:
          - name: file
            value: "{{item}}"
    - - name: collect-artifact
        template: collect-artifacts
        arguments:
          parameters:
            - name: processed
              value: "{{steps.process-artifact.outputs.parameters.processed}}"
    - - name: print-artifact
        template: print-message
        arguments:
          parameters:
          - name: file
            value: "{{steps.collect-artifact.outputs.result}}"

  - name: generate-artifacts
    inputs:
      parameters:
          - name: processed
    data:
      source:
        artifactPaths:
          s3:
            bucket: "test"
      transformation:
        - filter:
            regex: "\\.py"
        #- filter:
        #    regex: "lunch"
        - group:
            batch: 2
        #- group:
        #    regex: ".+?\\.(.+?)$"

  - name: collect-artifacts
    inputs:
      parameters:
        - name: processed
    data:
      transformation:
        - expression: "filter(data, {# > 3})"

  - name: process-message
    inputs:
      parameters:
      - name: file
    container:
      image: alpine:latest
      command: [sh, -c]
      args: ["echo \"{{inputs.parameters.file}}.done\" > /tmp/f.txt"]
    outputs:
      parameters:
        - name: processed
          valueFrom:
            path: /tmp/f.txt

  - name: print-message
    inputs:
      parameters:
      - name: file
    container:
      image: alpine:latest
      command: [sh, -c]
      args: ["echo {{inputs.parameters.file}}"]
