Description: Allow setting the namespace when submitting a `ClusterWorkflowTemplate`
Author: [Mason Malone](https://github.com/MasonM)
Component: UI
Issues: 10398

You can now specify the namespace when submitting a workflow from a `ClusterWorkflowTemplate` using an input field on the "Submit Workflow" panel.