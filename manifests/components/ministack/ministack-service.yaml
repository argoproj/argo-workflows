apiVersion: v1
kind: Service
metadata:
  name: ministack
  labels:
    app: ministack
spec:
  selector:
    app: ministack
  ports:
    - port: 4566
      name: api
      protocol: TCP
      targetPort: 4566
