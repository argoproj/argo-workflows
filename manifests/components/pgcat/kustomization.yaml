apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

# Resources are defined below in the combined section

# Include workflow controller configuration for pgcat integration
resources:
  - argo-pgcat-config-secret.yaml
  - pgcat-deployment.yaml
  - pgcat-service.yaml
  - postgres-deployment.yaml
  - postgres-primary-service.yaml
  - postgres-secondary-service.yaml
  - postgres-tertiary-service.yaml

patches:
  - path: overlays/workflow-controller-configmap.yaml