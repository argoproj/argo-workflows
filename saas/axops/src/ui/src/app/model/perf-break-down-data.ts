export class PerfBreakDownData {
    time: number;
    data: number;
    name: string;
    id: string;

    // property indicates if a particular data point is for a system service
    is_system: boolean;
}
