apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../../manifests/quick-start/base/workflow-role.yaml
  - ../../../../manifests/base/agent
  - agent-ing.yaml

patchesStrategicMerge:
  - agent-deploy.yaml