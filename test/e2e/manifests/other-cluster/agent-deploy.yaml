apiVersion: apps/v1
kind: Deployment
metadata:
  name: agent
spec:
  template:
    spec:
      containers:
        - name: main
          args:
            - --secure=false
            - --loglevel=debug
          readinessProbe:
            httpGet:
              scheme: HTTP
          imagePullPolicy: Never
