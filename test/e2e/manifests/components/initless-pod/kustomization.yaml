# Component that enables init-less pod mode in the workflow controller
# ConfigMap. Layered on top of a profile by an `<profile>-initless` overlay.
apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

patches:
  - path: workflow-controller-configmap.yaml
