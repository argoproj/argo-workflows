apiVersion: apps/v1
kind: Deployment
metadata:
  name: argo-server
spec:
  template:
    spec:
      containers:
        - name: argo-server
          # TODO - revert this image
          image: argoproj/argocli:ui-v3
          args:
            - server
            - --loglevel=debug
            - --namespaced
