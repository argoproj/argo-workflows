apiVersion: argoproj.io/v1alpha1
kind: Workflow
metadata:
  generateName: script-tracing-
  labels:
    workflows.argoproj.io/test: "true"
spec:
  entrypoint: main
  templates:
    - name: main
      script:
        image: python:alpine
        command: [python]
        source: |
          print("hello from script")
