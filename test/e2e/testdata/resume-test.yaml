apiVersion: argoproj.io/v1alpha1
kind: Workflow
metadata:
  name: resume-test
  labels:
    argo-e2e: "true"
    selected: "true"
spec:
  suspend: true
  entrypoint: run-workflow
  templates:
    - name: run-workflow
      container:
        image: argoproj/argosay:v2
        args: [echo, ":) Hello Argo!"]
